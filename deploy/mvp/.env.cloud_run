# Cloud Run Environment Configuration
# These are set via --set-env-vars and --set-secrets in deployment

# Core Settings (set via env vars)
MVP_MODE=true
GCP_PROJECT_ID=roadtrip-mvp-prod
GOOGLE_AI_LOCATION=us-central1
GOOGLE_AI_MODEL=gemini-1.5-flash
GCS_BUCKET_NAME=roadtrip-mvp-prod-audio

# Secrets (pulled from Secret Manager)
# DATABASE_URL - PostgreSQL connection with Cloud SQL proxy
# SECRET_KEY - Application secret key
# JWT_SECRET_KEY - JWT signing key
# GOOGLE_MAPS_API_KEY - For maps and places
# OPENWEATHERMAP_API_KEY - For weather data
# TICKETMASTER_API_KEY - For event detection (Phase 2)
# RECREATION_GOV_API_KEY - For campground data (Phase 2)

# Optional (can be added later)
# REDIS_URL - Set if Memorystore is configured
# SENTRY_DSN - For error tracking
# GOOGLE_APPLICATION_CREDENTIALS - Automatically set by Cloud Run